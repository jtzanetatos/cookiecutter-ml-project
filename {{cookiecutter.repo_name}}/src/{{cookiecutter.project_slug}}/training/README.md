# Training

Training orchestration and callbacks.

- `loops.py` constructs the Trainer and runs fit.
- MLflow integration is handled via `{{cookiecutter.project_slug}}/integrations/mlflow.py` (Pattern A).
