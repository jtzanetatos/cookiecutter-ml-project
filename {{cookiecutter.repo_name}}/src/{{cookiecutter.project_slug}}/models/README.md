# Models

Model definitions and factories.

- `models/torch/` contains pure PyTorch modules.
- `models/lightning/` contains LightningModules (optional).
- `models/factory.py` builds the correct model/module from Hydra config.
