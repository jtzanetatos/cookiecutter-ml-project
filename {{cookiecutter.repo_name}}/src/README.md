# src/

Python package source code for this project.

- `{{cookiecutter.project_slug}}/` is the importable package (rename via cookiecutter).
- Training entrypoints live in `{{cookiecutter.project_slug}}/cli/`.
- Deployment (FastAPI, Docker, K8s, Triton) lives under `deployment/` (not here).
