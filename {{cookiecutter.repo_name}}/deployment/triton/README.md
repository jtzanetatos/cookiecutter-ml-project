# Triton Inference Server

This directory contains resources for deploying models to [NVIDIA Triton Inference Server](https://github.com/triton-inference-server/server).

## Structure

- `model_repository/`: The model store directory structure required by Triton.
- `Dockerfile`: (Optional) Custom server image if needed.
